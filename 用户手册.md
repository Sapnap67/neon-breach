# NEON//BREACH 用户手册

## 这是什么

这是一款使用 Python 制作的霓虹风俯视角生存射击游戏。玩家需要移动、射击、冲刺并选择强化，在不断增多的敌人中生存。

## 启动

1. Windows 电脑需要先安装 Python 3。
2. 双击 `start.bat`。
3. 第一次启动会自动建立项目专用环境并安装 Pygame，需要联网等待片刻。
4. 以后双击即可直接进入游戏。

主菜单可以按回车，也可以点击中间的 `INITIALIZE` 按钮开始。

## 操作

- `WASD` 或方向键：移动
- 鼠标：瞄准；按住左键射击
- `空格`：冲刺
- `Esc`：暂停或继续
- 升级界面：点击卡片，或按 `1`、`2`、`3`；也可用左右键选择后按回车
- 游戏结束后按 `R`、回车或空格，或者点击重启按钮：重新开始

## 新增战斗机制

- 连续击杀会累积连击并提高得分，2.6 秒没有击杀后连击清零。
- 每生存 45 秒会出现一个会发射扇形弹幕的 Boss。
- 敌人有概率掉落绿色治疗核心，拾取后恢复 20 点生命。
- 玩家受伤后有 0.65 秒无敌时间，闪烁期间不会连续掉血。
- 左上角 `DASH` 条充满后才能再次冲刺。
- `REGENERATION` 强化每 4 秒自动回血；每选一次，每轮回血量增加 2 点，可以叠加。
- `RESISTANCE` 每层减少 12% 受到的伤害，可以叠加，最高减伤 60%。
- 抗性达到 60% 后，`RESISTANCE` 会自动从升级选项池移除，不再占用卡片位置。

## 属性面板

右上角的 `CORE ATTRIBUTES` 会实时显示伤害、移动速度、每秒射速、弹丸数量、冲刺冷却、每 4 秒回血量和抗性。选择强化后，对应数值会立即更新。

## 数据

最高分和游玩次数保存在项目目录下的 `save.json`。删除该文件会重置记录，不会影响游戏程序。

## 代码导览

项目真正需要理解的主要 Python 文件只有两个：

### `main.py`：游戏本体

它负责打开窗口并让游戏持续运行，主要分为这些部分：

- 全局设置：窗口大小、颜色和存档位置。
- `Bullet`、`Enemy`：分别保存每颗子弹和每个敌人的数据。
- `Game.__init__()`：初始化 Pygame、窗口、字体和游戏状态。
- `Game.reset()`：开始新一局时清空敌人、子弹、分数等数据。
- `Game.spawn_enemy()`：选择屏幕边缘、敌人类型和随时间提高的属性。
- `Game.update(dt)`：每一帧处理移动、射击、追踪、碰撞、升级和死亡。
- `Game.draw_world()`：绘制战场、角色、子弹、敌人和状态条。
- `Game.draw()`：根据菜单、暂停、升级、失败等状态绘制覆盖界面。
- `Game.run()`：整个游戏的主循环，顺序是“读取输入、更新、绘制”。

### `game_logic.py`：独立规则

它保存玩家属性、六种升级效果、等级公式和随机升级选项。它不负责画面，因此可以不打开窗口直接测试。

### `tests/test_game_logic.py`：自动测试

它不是游戏功能，而是检查经验边界、生命升级和随机选项是否正确。修改规则以后运行测试，可以更早发现错误。

## 常见问题

- 首次启动失败：确认已安装 Python 3，并检查网络。
- 窗口卡顿：关闭占用性能较高的软件后重试。
- 想完全重装依赖：关闭游戏，删除 `.venv` 文件夹，再运行 `start.bat`。
- 键盘没有反应：先点击一次游戏窗口，再使用 WASD 或方向键。
- 如果游戏没有获得键盘焦点，窗口底部会显示 `CLICK THE GAME WINDOW TO ENABLE KEYS`。
- Windows 版还会直接读取物理按键，作为 Pygame 键盘输入失效时的备用方案。
